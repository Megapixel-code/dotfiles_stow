return {
   "rebelot/kanagawa.nvim",
   opts = {
      transparent = false,
      dimInactive = false,   -- dim inactive screen
      terminalColors = true, -- define vim.g.terminal_color_{0,17}
      theme = "dragon",
   },
}
