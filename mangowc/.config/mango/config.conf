
# [[ KEYBINDS ]]
# https://github.com/DreamMaoMao/mangowc/wiki#key-bindings

# utilities
bind=SUPER+SHIFT,r,reload_config,
bind=SUPER+SHIFT,q,killclient,

bind=NONE,XF86MonBrightnessUp,spawn,brightnessctl set +5%
bind=NONE,XF86MonBrightnessDown,spawn,brightnessctl set -5%


# apps
bind=SUPER,Return,spawn,kitty
bind=SUPER,b,spawn,firefox

bind=SUPER,d,spawn,wmenu-run -l 10


# movement
bind=SUPER,h,focusdir,left
bind=SUPER,j,focusdir,down
bind=SUPER,k,focusdir,up
bind=SUPER,l,focusdir,right
bind=SUPER+SHIFT,h,exchange_client,left
bind=SUPER+SHIFT,j,exchange_client,down
bind=SUPER+SHIFT,k,exchange_client,up
bind=SUPER+SHIFT,l,exchange_client,right

bind=SUPER,n,focusstack,next
bind=SUPER,p,focusstack,prev
bind=SUPER+SHIFT,n,exchange_stack_client,next
bind=SUPER+SHIFT,p,exchange_stack_client,prev

bind=SUPER+CTRL,h,focusmon,left
bind=SUPER+CTRL,j,focusmon,down
bind=SUPER+CTRL,k,focusmon,up
bind=SUPER+CTRL,l,focusmon,right

bind=SUPER,1,toggleview,1
bind=SUPER,2,toggleview,2
bind=SUPER,3,toggleview,3
bind=SUPER,4,toggleview,4
bind=SUPER,5,toggleview,5
bind=SUPER,6,toggleview,6
bind=SUPER,7,toggleview,7
bind=SUPER,8,toggleview,8
bind=SUPER,9,toggleview,9


bind=SUPER,f,togglefullscreen,
bing=SUPER,space,togglefloating,


# [[ OUTPUTS ]]
# name,mfact,nmaster,layout,transform,scale,x,y,width,height,refreshrate
monitorrule=DP-1,0.55,1,tile,0,1,1080,688,1920,1080,60
monitorrule=HDMI-A-1,0.55,1,vertical_tile,1,1,0,0,1920,1080,60


# [[ AUTOSTART ]]
exec-once=waybar
exec-once=sunsetr


